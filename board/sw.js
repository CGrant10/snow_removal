/* Staff board's worker. Its own scope, /board/, which is what lets
   Android install it alongside the customer app. */
self.APP_BASE = "../";
self.APP_SHELL = ["admin.js"];
importScripts("../sw-core.js");
